//NAME : Mantavya Anand
//PRN : 24070123059
//CLASS & BATCH : EnTC - A3


#include <iostream>
 using namespace std;
int main() {
    int num1, num2, num3;
    cout << "Enter number 1: " << endl;
    cin >> num1;
    cout << "Enter number 2: " << endl;
    cin >> num2;
    cout << "Enter number 3: " << endl;
    cin >> num3;
    if (num1 > num2 && num1 > num3) {
        cout << "Num 1 is the largest " << endl;
    }

    else if ( num2 > num1 && num2 > num3) {
        cout << "Num 2 is the largest " << endl;
    }
    else {
        cout << "Num 3 is the largest " << endl;
    }
    }

